# TODO

- [x] Update simulation in `ApiService._startSimulation()` to also evolve `history7d` and `history30d` (currently only `history1d` updates).
- [ ] Keep `change24h` / `high24h` / `low24h` logic consistent.
- [ ] Run `flutter analyze` and `flutter test` (if available) to confirm no compile errors.
- [ ] Sanity check graph timeframe switching (1D/7D/30D) shows correct candle sequences and labels.


